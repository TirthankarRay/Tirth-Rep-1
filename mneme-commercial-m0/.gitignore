__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.env
*.log
.pytest_cache/
.mypy_cache/
.ruff_cache/

node_modules/
dist/
build/
.vite/
*.tsbuildinfo

.DS_Store
.idea/
.vscode/

# Postgres data volume
postgres-data/

# Local-only operator overrides
.local/

# The vault is a *runtime* git repo — initialized on `make seed`. We track
# its markdown contents in the outer monorepo but never the inner .git/.
vault/.git/

